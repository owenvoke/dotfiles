#!/usr/bin/env bash

# Loop through directories
for dir in *; do
    if [[ -d "${dir}/.git" ]]; then
        echo ${dir}
        # Pull the current branch from the origin
        git -C ${dir} pull
    fi
done
