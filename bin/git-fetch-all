#!/usr/bin/env bash

# Loop through directories
for dir in *; do
    if [[ -d "${dir}/.git" ]]; then
        echo ${dir}
        # Fetch changes from all remotes
        git -C ${dir} fetch --all
    fi
done
