#!/usr/bin/env bash

if [[ "$1" = '' ]]; then
    echo 'Usage: create-package {directory_name}'
    exit
fi

main() {
    git clone git@github.com:pxgamer/skeleton "$1" && \
    cd "$1" && \
    php prefill.php && \
    rm prefill.php && \
    rm -rf .git
}

main "$1"

